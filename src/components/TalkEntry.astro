---
import { type CollectionEntry, render } from "astro:content";

type Props = { talk: CollectionEntry<"talk"> };

const { talk } = Astro.props;
const { Content } = await render(talk);
---

<Content />
